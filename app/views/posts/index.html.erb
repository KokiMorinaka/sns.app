<h1>hello ruby</h1>

<ul>
  <% @posts.each do |post| %>
    <li>
      <h2><%= post.title %></h2>
      <p><%= post.content %></p>
      <span>
        <small><%= post.created_at %></small>
      </span>
    </li>
    <!== 開発段階でわかりやすくするためのサンプル ==%>
    <%= link_to "新規投稿一覧", "http://localhost:3000/posts/new" %>
  <% end %>
</ul>

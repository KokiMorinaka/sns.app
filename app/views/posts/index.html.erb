<div class="form-container">
<div class="button-container">
</div>
<ul>
  <% @posts.each do |post| %>
    <li>
      <p><%= post.title %></p>
      <p><%= post.recruitment_number %></p>
      <p><%= post.deadline %></p>
      <p><%= post.content %></p>
      <span>
        <small><%= post.created_at %></small>
      </span>
      <div class="edit-delete-wrapper">
        <%= link_to "編集", edit_post_path(post), class: "link-button" %>
        <%= button_to "削除", post_path(post), method: :delete, class: "link-delete-button" %>
      </div>
    </li>
  <% end %>
</div>
</ul>
</div>

<!--h1>掲示板アプリ</h1-->
<%#= link_to "新しい投稿", "http://localhost:3000/posts/new", class: "link-button" %>